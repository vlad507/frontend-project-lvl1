name: brainCI

on: 
  -push
jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [13.x]
    
    steps:
      - name: Run linter
        run: |
          make lint
